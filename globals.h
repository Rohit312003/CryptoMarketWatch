#pragma once
//these are global ptr so we can use it by including this file
class mainWindow;
class WebSocketConnection;
extern mainWindow*      MAIN_WWINDOW_PTR;
extern WebSocketConnection* WEB_SOCKET_CONNECTION;

